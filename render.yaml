
services:
  - type: web
    name: telegram-snapshot-backend
    env: python
    buildCommand: ""
    startCommand: "python tg_login_bot_backend.py"
    plan: free
    envVars:
      - key: BOT_TOKEN
        value: YOUR_TELEGRAM_BOT_TOKEN
